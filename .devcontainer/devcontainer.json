{
  "name": "algorithms-dev",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "cmake --preset gcc-debug && cmake --build --preset gcc-debug -j && ctest --preset all-gcc-debug --output-on-failure",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools"
      ]
    }
  }
}