//
// Created by andreas on 13.03.22.
//
#include <string>

bool are_strings_rotated(std::string & input1, std::string &input2)
{
	if (input1.empty() && input2.empty())
		return true;
	if (input1.size() != input2.size())
		return false;


	for(size_t index1 = 0; index1 < input1.size(); ++index1)
	{

	}
}

int main()
{

}